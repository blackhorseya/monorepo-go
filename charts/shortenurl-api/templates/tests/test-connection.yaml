apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shortenurl-api.fullname" . }}-test-connection"
  labels:
    {{- include "shortenurl-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shortenurl-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
