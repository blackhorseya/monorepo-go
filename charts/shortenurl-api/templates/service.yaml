apiVersion: v1
kind: Service
metadata:
  name: {{ include "shortenurl-api.fullname" . }}
  labels:
    {{- include "shortenurl-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shortenurl-api.selectorLabels" . | nindent 4 }}
